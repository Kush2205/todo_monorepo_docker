{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "start:http-backend": "bun run prisma:migrate && bun run apps/http-backend/src/index.ts",
    "start:ws-backend": "bun run prisma:migrate && bun run apps/ws-backend/src/index.ts",
    "start:frontend" : "bun run prisma:migrate && cd apps/frontend && bun run start",
    "prisma:migrate" : "cd packages/db && bun prisma migrate dev",
    "prisma:generate": "cd packages/db && bun prisma generate"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
